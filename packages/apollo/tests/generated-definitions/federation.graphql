type Post {
  id: ID!
  title: String!
  author: User!
  category: Category!
}

extend type Query {
  getPosts: [Post]
}

extend type User @key(fields: "id") {
  id: ID! @external
  posts: [Post]
}

extend enum Category {
  POST
}
